package service;

import dao.AgendaDao;
import model.AgendamentoVisita;

import java.util.Collections;
import java.util.List;

public class AgendaService {
    /*
    public void salvar(AgendamentoVisita agendamento) {
        try {
            AgendaDao dao = new AgendaDao();

            if (agendamento.getId() == 0) { // Verifica se é um novo agendamento ou edição
               // dao.inserir(agendamento); // Insere o novo agendamento
            } else {
               // dao.editar(agendamento); // Atualiza o agendamento existente
            }
        } catch (Exception e) {
            System.out.println("Erro ao salvar agendamento: " + e.getMessage());
        }
    }

    public List<AgendamentoVisita> listarTodos() {
        try {
            AgendaDao dao = new AgendaDao();
           // return dao.listarTodos();
        } catch (Exception e) {
            System.out.println("Erro ao listar agendamentos: " + e.getMessage());
            return Collections.emptyList();
        }
    }

    public AgendamentoVisita buscarPorId(int id) {
        try {
            AgendaDao dao = new AgendaDao();
          //  return dao.buscarPorId(id);
        } catch (Exception e) {
            System.out.println("Erro ao buscar agendamento por ID: " + e.getMessage());
            return null;
        }
    }

    public void deletar(int id) {
        try {
            AgendaDao dao = new AgendaDao();
          //  dao.deletar(id);
        } catch (Exception e) {
            System.out.println("Erro ao deletar agendamento: " + e.getMessage());
        }
    }
*/
}

